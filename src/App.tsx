import React, { FC } from 'react';
import './App.css';
import Cart from './pages/cart';

const App: FC = () => {
    return (
        <main>
            <Cart />
        </main>
    );
};

export default App;
